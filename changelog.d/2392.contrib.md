The `Notify Codecov` CI workflow job now only runs in the upstream
project repository. This fixes a problem contributors face when running
CI in their forks that lack Codecov API access privileges.

-- {user}`gaborbernat`
